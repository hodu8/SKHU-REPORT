/*
 * 주제 : Person, Dog 클래스를 정의하는 프로그램
 * 작성일 : 2018.05.24.
 * 작성자 : 박찬우
 */

package hw9_1;

public class SpeakableTest {

	public static void main(String[] args) {
		System.out.println("hw9_1 : 박찬우\n");
		Speakable[] s = new Speakable[4]; // 크기가 4인 Speakble 배열을 생성
		s[0] = new Person("강감찬");
		s[1] = new Person("홍길동");
		s[2] = new Dog(2);
		s[3] = new Dog(5);
		// Person 객체 2개, Dog 객체 2개를 생성하여 배열에 저장
		
		for (int i=0; i < 4; i++) {
			s[i].speak();
			System.out.println("\n");
		} // 반복문을 이용하여 모든 배열 원소 객체에 대해 speak()를 호출하고,
		  // 만일 배열 원소가 사람이면 추가로 이름을 조회(getName() 호출)하여 출력
	}

}
